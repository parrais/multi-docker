name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t mlewissparta/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true mlewissparta/react-test npm test

      - run: docker build -t mlewissparta/multi-client ./client
      - run: docker build -t mlewissparta/multi-nginx ./nginx
      - run: docker build -t mlewissparta/multi-server ./server
      - run: docker build -t mlewissparta/multi-worker ./worker

      - run: docker push mlewissparta/multi-client
      - run: docker push mlewissparta/multi-nginx
      - run: docker push mlewissparta/multi-server
      - run: docker push mlewissparta/multi-worker
